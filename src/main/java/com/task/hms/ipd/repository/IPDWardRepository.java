package com.task.hms.ipd.repository;

import com.task.hms.ipd.model.IPDWard;
import org.springframework.data.jpa.repository.JpaRepository;

public interface IPDWardRepository extends JpaRepository<IPDWard, Long> {
}
